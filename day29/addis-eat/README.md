# Addis Eats Menu

A static restaurant menu built with React and Vite. This project demonstrates component composition, passing props, and rendering lists using `.map()` with unique keys in React.

## Prerequisites
- Node.js installed on your machine.

## How to Run

1. Clone this repository (if pulling from GitHub):
   ```bash
   git clone <your-repo-url>